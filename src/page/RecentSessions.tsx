import { StyleSheet, Text, View } from "react-native";
import React from "react";

type Props = {};

export const RecentSessions = (props: Props) => {
  return (
    <View>
      <Text>RecentSession</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
